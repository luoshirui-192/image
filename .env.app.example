# 机器 A 环境变量（复制为项目根目录 .env）
# cp .env.app.example .env

# ========== 本机（机器 A）==========
# 浏览器访问地址：填机器 A 的 IP 或域名，不要末尾斜杠
PUBLIC_URL=http://192.168.1.10

# 对外 HTTP 端口
HTTP_PORT=80

# ========== MySQL（本机 Docker 容器）==========
MYSQL_ROOT_PASSWORD=change_me_root
MYSQL_PASSWORD=change_me_app
MYSQL_DATABASE=image_db
MYSQL_USER=image_db

# 仅本机调试时可映射 3306；生产建议保持 127.0.0.1 绑定（compose 默认）
MYSQL_PUBLISH_PORT=3306

# ========== 图片存储（来自机器 B 的 NFS）==========
# 启动前将 B 的 upload 挂载到本项目 ./upload，例如：
# sudo mount -t nfs 192.168.1.20:/data/image_db/upload /opt/image_db/upload
MACHINE_B_NFS_HOST=192.168.1.20
MACHINE_B_NFS_PATH=/data/image_db/upload

# ========== 安全密钥（生产务必修改）==========
# 生成：python3 scripts/generate_secret_key.py
SECRET_KEY=change-me-docker-secret-key
IMAGE_ACCESS_SECRET=change-me-docker-image-secret

# ========== 定时维护（scheduler 容器）==========
LOG_RETENTION_DAYS=90
DELETED_IMAGE_RETENTION_DAYS=30
MAINTENANCE_INTERVAL_HOURS=24

# ALLOWED_HOSTS / CORS_ALLOWED_ORIGINS 由 start-app.sh 根据 PUBLIC_URL 自动写入
