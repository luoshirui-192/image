# 第 7 章：前端入口、路由与布局

---

## `frontend/index.html`（13 行）

| 行 | 说明 |
|----|------|
| `<div id="app">` | Vue 挂载点 |
| script src `/src/main.js` | Vite 入口 |

---

## `frontend/src/main.js`（22 行，逐行）

| 行 | 说明 |
|----|------|
| 1-5 | 导入 Vue、Pinia、Element Plus 及中文 locale、样式 |
| 7-10 | App 根组件、router、guards、`main.css` |
| 12-13 | 创建 app 与 pinia 实例 |
| 15-17 | 注册 pinia、router、ElementPlus |
| 19-21 | `bootstrapAuth()` 恢复登录态后再 `mount`，避免闪登录页 |

---

## `frontend/src/App.vue`（5 行）

仅 `<router-view />` — 所有页面由路由切换。

---

## `frontend/vite.config.js`

| 配置 | 说明 |
|------|------|
| `@` alias | 指向 `src/` |
| `server.proxy /api` | 开发时转发到 Django 8000 |

---

## `frontend/src/router/index.js`（92 行）

| 行块 | 说明 |
|------|------|
| 16-23 | `VIEW_MAP`：菜单 name → 懒加载组件 |
| 25-36 | `buildChildRoutes`：按 `MENU_ITEMS` 生成子路由 |
| 37-48 | **隐藏路由** `/categories`（不在侧栏） |
| 49-58 | 旧路径重定向：`blob-views`、`sql-query` → `blob-browse` |
| 55-75 | 根布局 `MainLayout`，children 为各页 |

---

## `frontend/src/router/guards.js`（50 行）

| 函数/逻辑 | 说明 |
|-----------|------|
| `beforeEach` | 非 public 路由需 token；adminOnly 需 isAdmin |
| 401 处理 | 在 request.js，不在 guards |
| `bootstrapAuth` | 有 token 无 user 时调 `/me` |

---

## `frontend/src/config/menu.js`（83 行）

| 项 | path | 页面 |
|----|------|------|
| home | `` | 首页 |
| upload | upload | 上传 |
| blob-migrate | blob-migrate | 迁移 |
| blob-browse | blob-browse | 数据库模拟 |
| logs | logs | 日志（admin） |
| settings | settings | 设置（admin） |

**已移除** categories 菜单项；`resolveActiveMenuPath(path, query)` 在 `/categories?from=upload` 时高亮上传。

---

## `frontend/src/layouts/MainLayout.vue`（387 行，分块）

| 行块 | 说明 |
|------|------|
| script | 侧栏折叠、移动端抽屉、`filterMenuByRole` 菜单 |
| `activeMenu` | 当前高亮菜单项 |
| template 侧栏 | `el-menu` + `router-link` 每项 |
| 顶栏 | 用户名、退出、面包屑 `AppBreadcrumb` |
| 主区 | `<router-view />` |
| style | 侧栏 fixed、内容区 margin（之前修滚动 bug） |

---

## `frontend/src/stores/auth.js`（86 行，逐行）

| 行 | 说明 |
|----|------|
| 5-7 | localStorage key 常量 |
| 10-12 | ref：access、refresh、user |
| 14-16 | computed：是否登录、是否 admin、用户名 |
| 18-25 | `loadUser` 解析 JSON |
| 27-34 | `persistSession` 登录成功后写入 localStorage |
| 36-43 | `clearSession`  logout |
| 45-49 | `login` 调 API + persist |
| 51-56 | `fetchMe` 刷新用户信息 |
| 58-66 | `refreshAccessToken` 用 refresh 换新 access |
| 68-70 | `logout` |
| 72-85 | 导出给组件用 |

---

## `frontend/src/api/request.js`（76 行，逐行）

| 行 | 说明 |
|----|------|
| 6-9 | axios 实例，baseURL `/api`，30s 超时 |
| 11 | 防止并发 refresh 重复 |
| 13-24 | 请求拦截：加 Bearer；FormData 不设 Content-Type |
| 26-38 | 响应拦截：`code!==0`  reject |
| 40-58 | 401 时 refresh 并重试原请求 |
| 61-71 | 其他错误 ElMessage + reject |

---

## `frontend/src/api/images.js`（292 行）

按功能分块阅读：

| 行块约 | 函数 |
|--------|------|
| 开头 | `listCategoriesApi` / CRUD |
| | `uploadImagesApi` FormData |
| | `fetchImageBlob` 拉取二进制预览 |
| 中部 | blob-migration sources/jobs/run |
| 后部 | blob-catalog、blob-browse table views |

每个 export 函数 = 一个 REST 端点的薄封装。

---

## `frontend/src/components/AppBreadcrumb.vue`

根据 `route.path` 与 `route.meta.title` 生成面包屑；隐藏页显示父菜单（upload/迁移）。

---

下一章：[08-前端页面组件上.md](./08-前端页面组件上.md)
