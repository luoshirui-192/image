# 第 8 章：前端页面（上）

---

## `Login.vue`（161 行）

| 块 | 说明 |
|----|------|
| form | username/password |
| submit | `auth.login` → 读 redirect query → `router.push` |
| 校验 | 非空、错误提示 |

---

## `Home.vue`（168 行）

| 块 | 说明 |
|----|------|
| `healthApi` | 展示后端版本/状态 |
| `quickMenus` | 前 4 个非 home 菜单快捷卡片 |
| 文案 | 按 admin/普通用户不同 hint |

---

## `Upload.vue`（435 行）

### script 分块

| 行约 | 内容 |
|------|------|
| 16-17 | 允许扩展名、20MB 限制 |
| 19-33 | refs：文件列表、分类、标签、进度 |
| 37-40 | `canUpload` 计算属性 |
| 42-66 | 本地预览 URL、beforeUpload 校验 |
| 83-93 | `loadCategories` |
| 95-103 | 新建分类弹窗 / **goManageCategories** 跳 `/categories?from=upload` |
| 125-180 | `submitUpload`：循环 `uploadImagesApi` |
| onMounted | loadCategories |

### template 分块

| 块 | 说明 |
|----|------|
| 分类行 | select + 新建 + **管理** |
| el-upload | drag、多选、`:auto-upload="false"` 手动提交 |
| 结果表 | 成功/失败、ImagePreview |

---

## `CategoryManage.vue`（209 行）

| 块 | 说明 |
|----|------|
| `backTarget` | 根据 query.from 决定返回上传/迁移/首页 |
| table | 分类列表 CRUD |
| dialog | 新增/编辑 name+sort |
| delete | 有图片时后端 400 |

**注意：** 第 177 行必须是 `</el-dialog>`（曾误写 `</dialog>` 导致构建失败）。

---

## `Settings.vue`（182 行）

| 块 | 说明 |
|----|------|
| GET/PATCH `/api/config/` | 最大上传、保留天数等 |
| adminOnly | 路由 meta 限制 |

---

## `Logs.vue`（221 行）

| 块 | 说明 |
|----|------|
| 筛选 | operate_type、日期 |
| 表格 | 分页日志 |
| `api/logs.js` | OPERATE_TYPES 下拉标签 |

---

## `PagePlaceholder.vue`（55 行）

未映射菜单时的占位；正常不应到达。

---

## 公共组件速览

### `ImagePreview.vue`（173 行）

| 块 | 说明 |
|----|------|
| props | `imageId` / `imagePath` / `size` |
| watch | 变化时 `fetchImageBlob` |
| 显示 | img 或 loading/error |

### `SqlEditor.vue`（108 行）

CodeMirror 6 封装；`v-model` 绑定 SQL 文本；Ctrl+Enter 触发 execute 事件。

---

下一章：[09-前端页面组件下.md](./09-前端页面组件下.md)
